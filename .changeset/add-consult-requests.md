---
"peer-rtc-api": minor
---

Add consult request APIs for consumers and providers, with Prisma models for consult requests and call sessions. Move auth into `src/modules` and store timestamps as native DateTime values.
